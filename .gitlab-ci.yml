stages:
  - test

test_front:
  stage: test
  image: selenium/standalone-chrome:latest
  before_script:
    - apt-get update
    - apt-get install -y robotframework robotframework-seleniumlibrary
  script:
    - robot -d results robot.robot
  artifacts:
    paths:
      - results/
  only:
    - developer
